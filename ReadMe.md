Пет-проект: парсер вакансий на разных сервисах
стэк: голанг (пока консольная реализация)

что сделано:

- создан каркас проекта
- реализован механизм поиска вакансий на 2х источниках (HH.ru, SuperJob.ru) (parser manager, можно добавлять новые источники поиска вакансий)
- реализован sharded inmemory cache - для поиска
- реализован sharded inmemory cache - для получения конкретной ваканси по ID из первого кэша (используем обратный индекс)
- реализован rate limiter для каждого парсера (ограничение частоты запросов на ресурс)
- реализован адекватный поиск по ID среди закэшированных результатов (кэш с обратным индексом)
- реализован паттерн "семафор" для каждого парсера (для ограничения параллельности)
- реализован circuit breaker для каждого парсера (для блокировки недоступного сервиса)
- реализован механизм создания парсеров через паттерн "фабрика"
- отрефакторина логика получения конфиг-данных (из .env и из .yml)
- реализован глобальный Circuit Breaker - мониторинг общего здоровья [в parse manager] (**_ необходимо нагрузочное тестирование _**)
- реализована очередь для менеджера парсеров, создан механизм воркеров, которые разгребают эту очередь
- реализован глобальный семафор - ограничение одновременных поисков [в parse manager] (контроль ресурсов сервера)
- реализован конфиг для клиента, через который будет усуществляться проверка HealthCheck парсеров
- реализован клиент в составе менеджера статусов, который входит в состав менеджера парсеров
- реализован механизм обновления статусов парсеров (при первом запуске и дальше циклическое обновление через заданное в крнфиге время)

перспектива:

- FIFO очередь построена на дженериках, нужно передавать интерфейс джобы (в очередь будут прилетать джобы разных типов, но
  удовлетворяющие одному интерфейсу). Одни джобы-поиск списка вакансий, другие - получение более полных данных в конкретном сервисе по конкретному ID.
- реализовать валидаию данных из источников config
- реализовать механизм восстановления данных при запросе ID, если в основном кэше данных нет ("lazy-валидация с фолбэком")
- реализовать Load balancer - распределение между источниками [в parse manager]
- реализовать Resource manager - контроль памяти/CPU [в parse manager]
- отредакторить код (разделить полностью на слои: DTL, service, repository)
- конкурентный парсинг из произольного количества источников
- логирование
- тесты

Планируемая архитектура:
Пользовательский запрос
↓
┌─────────────────────────────────┐
│ PARSER MANAGER УРОВЕНЬ ← Глобальный контроль
│ • Глобальный семафор
│ • Приоритетная очередь
│ • Load balancer между парсерами
│ • Общий circuit breaker
└─────────────────────────────────┘
↓
┌─────────────────────────────────┐
│ PARSER УРОВЕНЬ (HH/SuperJob) ← Индивидуальный контроль
│ • Индивидуальный семафор
│ • Rate limiter источника
│ • Circuit breaker источника
└─────────────────────────────────┘
↓
Внешние API

repo: https://github.com/AnumSmart/Job_parser.git
